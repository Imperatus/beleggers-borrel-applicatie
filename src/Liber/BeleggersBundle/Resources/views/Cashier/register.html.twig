{% extends 'base.html.twig' %}

{% block head %}

{% endblock %}

{% block title 'SpiritStock' %}

    {% block body %}
    {#{% for flashMessage in app.session.flashbag.get('notice') %}
        <div class="flash-message">
            <em>Notice</em>: {{ flashMessage }}
        </div>
    {% endfor %}#}

    {% block content_header %}
        <div class="row-fluid">
            <div class="span12">
                <div class="navbar">
                    <div class="navbar-inner">
                        <a class="brand" href="#">SpiritStock v0.1a</a>
                        <ul class="nav">
                            <li><a href="{{ path('liber_beleggers_settings') }}">{{ 'menu.settings'|trans }}</a></li>
                            <li class="active"><a href="#">{{ 'menu.cashier'|trans }}</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    {% endblock %}

    <div class="row-fluid">
        <div class="span9">
            <div class="well">
                {% block content %}

                {% endblock %}
            </div>
        </div>
        <div class="span3">
            <div class="well">
                {% block orderbar %}

                {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}